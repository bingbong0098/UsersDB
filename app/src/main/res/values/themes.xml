<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UsersDB" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->


        <!-- Text color for normal text -->
        <item name="android:textColor">@color/textColor</item>

        <!-- EditText text color -->
        <item name="android:editTextColor">@color/editTextColor</item>

        <!-- EditText hint color -->
        <item name="android:textColorHint">@color/colorHint</item>

        <!-- Button color -->
        <item name="android:colorButtonNormal">@color/colorButtonLight</item>
        <item name="android:button">@color/colorButtonLight</item>

        <!-- Switch button color -->
        <item name="colorControlActivated">@color/colorSwitchLight</item>
        <item name="colorControlNormal">@color/colorSwitchLight</item>



        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.UsersDB" parent="Base.Theme.UsersDB" />



</resources>